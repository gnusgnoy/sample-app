<h1>EMS Data</h1>

<table>
  <thead>
    <tr>
      <th id="Data Name"></th>
      <th id="Node"></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @waters.each do |water| %>
      <tr>
        <td><strong>Node Name: <%= water.node %></td>
        <td><%= water.dgm %></td>
        <td><%= link_to 'Show', water %></td>
        <td><%= link_to 'Edit', edit_water_path(water) %></td>
        <td><%= link_to 'Destroy', water, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Add new node', new_water_path %>
